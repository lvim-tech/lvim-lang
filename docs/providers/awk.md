# AWK

A declarative Tier 3 provider (scripting) — data record in `lvim-lang.providers.registry.awk`. awk-language-server is the LSP. `awk -f` runs a script.

## LSP

`awk-language-server` (mason).

## Per-filetype catalog

| Filetype | Formatters | Linters | Debuggers | Defaults |
| --- | --- | --- | --- | --- |
| `awk` | — | — | — | — |

## Commands

| Command | Description |
| --- | --- |
| `:LvimLang run` | `awk -f <file>` |

## Notes

AWK has no standard test framework or debugger — none is offered.
