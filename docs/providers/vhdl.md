# VHDL

A declarative Tier 3 provider (HDL) — data record in `lvim-lang.providers.registry.vhdl`. rust_hdl's vhdl_ls is the LSP.

## LSP

`rust_hdl` (mason, bin `vhdl_ls`).

## Per-filetype catalog

| Filetype | Formatters | Linters | Debuggers | Defaults |
| --- | --- | --- | --- | --- |
| `vhdl` | — | — | — | — |

## Commands

| Command | Description |
| --- | --- |
| — | — |

## Notes

HDL verification (VUnit / cocotb) is simulator-driven and project-specific — no generic test adapter or DAP is offered.
