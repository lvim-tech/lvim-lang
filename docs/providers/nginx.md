# Nginx

A declarative Tier 3 provider (config / DSL) — data record in `lvim-lang.providers.registry.nginx`. nginx-language-server provides completion / hover / diagnostics for nginx.conf and vhost files.

## LSP

`nginx-language-server` (mason).

## Per-filetype catalog

| Filetype | Formatters | Linters | Defaults |
| --- | --- | --- | --- |
| `nginx` | — | — | — |

## Commands

| Command | Description |
| --- | --- |
| `:LvimLang test` | `nginx -t` (config test) |
